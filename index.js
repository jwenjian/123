var scrolled = false;
var canScroll = false;

function bodyScroll(e) {
  console.log(el)
  scrolled = true
  if (!canScroll) {
    alert('dead')
  }
}

window.onload = function(e) {
  
}
